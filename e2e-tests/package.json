{
  "name": "e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for PRISM Analytics file upload and database persistence",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "dependencies": {
    "@playwright/test": "^1.40.0"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}